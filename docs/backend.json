
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user profile within the Timgad social media application.",
      "properties": {
        "id": { "type": "string" },
        "username": { "type": "string" },
        "bio": { "type": "string" },
        "profilePictureUrl": { "type": "string", "format": "uri" },
        "bannerUrl": { "type": "string", "format": "uri" },
        "createdAt": { "type": "string", "format": "date-time" },
        "lastLoginAt": { "type": "string", "format": "date-time", "description": "Used for daily login rewards." },
        "lastPollCreatedAt": { "type": "string", "format": "date-time" },
        "followingIds": { "type": "array", "items": { "type": "string" } },
        "followerIds": { "type": "array", "items": { "type": "string" } },
        "interactedAuthorIds": { "type": "array", "items": { "type": "string" }, "description": "List of author UIDs this user has interacted with for recommendation scoring." },
        "email": { "type": "string", "format": "email" },
        "role": { "type": "string", "enum": ["user", "admin"] },
        "verificationType": { "type": "string", "enum": ["none", "blue", "gold"] },
        "verificationExpiresAt": { "type": "string", "format": "date-time" },
        "isPrivate": { "type": "boolean", "default": false },
        "coins": { "type": "number", "default": 0 }
      },
      "required": ["id", "username", "createdAt"]
    },
    "Post": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Post",
      "type": "object",
      "description": "Represents a user post in Timgad, potentially including a poll.",
      "properties": {
        "id": { "type": "string" },
        "authorId": { "type": "string" },
        "authorName": { "type": "string" },
        "authorAvatar": { "type": "string" },
        "content": { "type": "string" },
        "mediaUrls": { "type": "array", "items": { "type": "string" } },
        "mediaType": { "type": "string", "enum": ["image", "video"] },
        "poll": {
          "type": "object",
          "properties": {
            "question": { "type": "string" },
            "options": {
              "type": "array",
              "items": {
                "type": "object",
                "properties": {
                  "text": { "type": "string" },
                  "votes": { "type": "number", "default": 0 }
                }
              }
            },
            "totalVotes": { "type": "number", "default": 0 },
            "expiresAt": { "type": "string", "format": "date-time" }
          }
        },
        "createdAt": { "type": "string", "format": "date-time" },
        "hashtags": { "type": "array", "items": { "type": "string" } },
        "likesCount": { "type": "number", "default": 0 },
        "commentsCount": { "type": "number", "default": 0 },
        "repostsCount": { "type": "number", "default": 0 },
        "viewsCount": { "type": "number", "default": 0 },
        "reportsCount": { "type": "number", "default": 0 },
        "promoted": { "type": "boolean" },
        "impressions_left": { "type": "number" }
      },
      "required": ["id", "authorId", "content", "createdAt"]
    },
    "Report": {
      "title": "Report",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "reporterId": { "type": "string" },
        "targetId": { "type": "string" },
        "targetType": { "type": "string", "enum": ["post", "user"] },
        "reason": { "type": "string" },
        "status": { "type": "string", "enum": ["pending", "resolved"] },
        "createdAt": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "structure": [
      { "path": "users/{userId}", "definition": { "entityName": "User" } },
      { "path": "posts/{postId}", "definition": { "entityName": "Post" } },
      { "path": "posts/{postId}/likes/{userId}", "description": "Users who liked this post." },
      { "path": "posts/{postId}/comments/{commentId}", "description": "Comments on this post." },
      { "path": "posts/{postId}/pollVotes/{userId}", "description": "Individual votes for polls." },
      { "path": "reports/{reportId}", "definition": { "entityName": "Report" } },
      { "path": "users/{userId}/supportedPeople/{targetUserId}", "description": "List of people supported by this user." }
    ]
  }
}
